export enum ROLES {
  moderator = "moderator",
  admin = "admin",
  user = "user",
}

export enum AuthReducerAction {
  LOGIN = "LOGIN",
  LOGOUT = "LOGOUT",
  UPDATE_USER_NAME = "UPDATE_USER_NAME",
}
